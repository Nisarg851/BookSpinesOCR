# Copy to .env and restart Expo after changing this.
# Simulators: you can leave this unset (see App defaults).
# Physical device: use your computer's LAN IP, not localhost.
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
