# Copy this file to .env (same folder), then restart Expo.
# Physical phone: your PC's Wi-Fi IP, not 127.0.0.1.
# Find it: Windows → Settings → Network → Wi-Fi → Properties → IPv4
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
